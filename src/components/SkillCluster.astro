---
import TechTag from '@components/TechTag.astro';

interface Props {
  title: string;
  description: string;
  items: string[];
  dark?: boolean;
}

const { title, description, items, dark = false } = Astro.props;
---

<article class={dark ? 'rounded-lg border border-white/10 bg-white/5 p-5' : 'surface-card'}>
  <h3 class={dark ? 'font-semibold text-[color:var(--cream)]' : 'font-semibold text-[color:var(--ink)]'}>{title}</h3>
  <p class={dark ? 'mt-3 text-sm leading-7 text-[color:var(--warm-muted)]' : 'mt-3 text-sm leading-7 text-[color:var(--muted)]'}>{description}</p>
  <div class="mt-4 flex flex-wrap gap-2">
    {items.map((item) => <TechTag label={item} tone={dark ? 'dark' : 'default'} />)}
  </div>
</article>
